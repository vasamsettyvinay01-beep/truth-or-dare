# Vercel Project Settings (monorepo)
# Root Directory can stay `.` with vercel.json buildCommand above.
# Or set Root Directory to `web` in the Vercel dashboard and use web/package.json scripts.

NEXT_PUBLIC_SOCKET_URL=
